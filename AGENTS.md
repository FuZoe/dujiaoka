# Working Instructions

- Work only in `C:\GitRepos\dujiaoka-telegram-restock` for source changes.
- Preserve the existing NewZoe shop behavior and deployed data.
- Never print, commit, or expose Telegram tokens, SMTP credentials, passwords, or production environment files.
- Implement, test, deploy, verify, commit, push, and open a reviewable pull request without waiting for confirmation.
- When a proposed approach fails, diagnose it and continue with a different implementation.
- Treat `122.51.54.240` and `/opt/dujiaoka` as the production deployment target. Back up changed production files before replacement.
